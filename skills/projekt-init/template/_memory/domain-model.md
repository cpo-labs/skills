# {{PROJECT}} — Domain Model

> Structural knowledge about the project: entities, relations, constraints,
> invariants. Pulled into context via the `@`-pointer in `CLAUDE.md`, so it is
> always loaded.

_TBD — fill in once domain knowledge exists_
